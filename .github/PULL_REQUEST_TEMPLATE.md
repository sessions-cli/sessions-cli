## Summary

What does this change do?

## Test plan

- [ ] `cargo test --locked`
- [ ] `cargo fmt -- --check`
- [ ] `cargo clippy --locked -- -D warnings`
- [ ] `make reload` (if Rust/UI behavior changed)
- [ ] Fresh `./install.sh` adoption path considered (if hooks, scripts, or paths changed)

## Notes

Call out breaking changes, follow-up work, or docs updates.